import SmartToyIcon from '@mui/icons-material/SmartToy';
import { SvgIcon, SvgIconProps } from '@mui/material';

const AIAssistantIcon = (props: SvgIconProps) => (
  <SvgIcon {...props}>
    <SmartToyIcon />
  </SvgIcon>
);

export default AIAssistantIcon;
